<% content_for :title do "Information about “#{@artefact["title"]}”" end %>

<%= render partial: "artefact", locals: { artefact: @artefact, show_needs: @show_needs, format: @format } %>
<%= render partial: "lead_metrics", locals: { lead_metrics: @lead_metrics, format: @format } %>
<%= render partial: "per_page_metrics", locals: { per_page_metrics: @per_page_metrics } %>
<%= render partial: "needs", locals: { show_needs: @show_needs, needs: @needs, format: @format } %>
